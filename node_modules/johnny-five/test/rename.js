// var fs = require("fs");
// var path = require("path");



// fs.readdir(__dirname, (error, files) => {
//   files.forEach(file => {
//     // var extPattern = /\.(mp4|ogv|webm)$/gm;
//     // var ext = path.extname(file);

//     if (path.extname(file) === ".js" && !file.includes("rename")) {
//       var contents = fs.readFileSync(file, "utf8");

//       if (contents.includes("mock-firmata")) {
//         console.log("Replace in: ", file);

//         contents = contents.replace(
//           'MockFirmata = require("mock-firmata").Firmata', 'mocks = require("mock-firmata"),\n  MockFirmata = mocks.Firmata');

//         fs.writeFileSync(file, contents);
//       }
//     }
//     // if ((/\.(mp4|ogv|webm)$/gm).test(file)) {
//     //   var dotIndex = file.indexOf(".");
//     //   var newName = file.slice(0, dotIndex) + ext;

//     //   fs.rename(path.join(__dirname, file), path.join(__dirname, newName));
//     // }

//   });
// });
